import React, {useEffect, useState} from 'react'
import tw from "tailwind.macro";
import styled from 'styled-components/macro'

const TextArea = styled.textarea`${tw`mb-3 block w-full max-w-2xl`}`
export default TextArea